<!DOCTYPE html>
<html>

<head>
  <title>wiphat</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
  <link rel="stylesheet" href="/css/style.css">
  <link href="https://fonts.googleapis.com/css?family=Lora" rel="stylesheet">
</head>

<body id="register">
  <div class="row pane" id="welcome">
    <div class="col-xs-12">
      <h2>wiphat</h2>
      <p>wiphat is an anonymous & non-tracking chat platform</p>
    </div>
  </div>

  <div class="row pane" id="start">
    <h2>let's start</h2>
    <div class="col-md-6 col-md-offset-3 col-xs-10 col-xs-offset-1">
      <span>create a private room</span>
    </div>
  </div>

  <div class="row pane" id="about">
    <h2>about</h2>
    <div class="col-md-6 col-md-offset-3 col-xs-10 col-xs-offset-1">
      <p>nodejs, ejs, socket.io<br>made with ❤ by kaanklky<br><br><a href="https://github.com/kaanklky/wiphat" target="_blank">github</a></p>
    </div>
  </div>

  </form>
  <script src="/js/jquery.js"></script>
  <script src="/js/web.js"></script>
  <script src="/socket.io/socket.io.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
  <script>
    $(document).ready(function () {
      var socket = io();

      // When the user submits name and room
      $('#start > div').on('click', function () {
        // get userName & room id from nodejs
        var name = '<%= userName %>';
        var room = '<%= room %>';

        // Submitted name and room name organized neatly
        var data = {
          name: name,
          room: room
        }

        // Tell the server that a new user has joined, and give it the users name and desired room
        socket.emit('new user', data);

        // Redirect the user to the actual chat
        window.location.href = "/chat/" + room;

        return false;
      });
    });
  </script>
</body>

</html>